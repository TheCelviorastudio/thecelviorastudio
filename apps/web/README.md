# thecelviorastudio — web

Next.js 16 storefront for thecelviorastudio. See the root README for workspace commands.

```sh
pnpm dev        # http://localhost:3000
pnpm build
pnpm lint
pnpm check-types
```
